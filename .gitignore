# ⚠️ ENVIRONMENT & SECRETS - CRITICAL: NEVER COMMIT THESE
.env
.env.local
.env.production
.env.production.local
.env.*.local
.env.*.example

# Firebase Configuration
.firebaserc
firebase-debug.log
secrets/
firebase-keys/
*.key
*.pem

# Sensitive Files
.env.vault
.env.enc

# Dependencies
node_modules/

# NOTE: package-lock.json is deliberately COMMITTED. This is an application, not
# a library: the lockfile is what makes builds reproducible, and `npm ci` (used
# by CI) refuses to run without one.

# Build outputs
dist/
dist-ssr/
functions/lib/
*.local

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# OS
Thumbs.db
*.pdb

# Development & Logs
*.log
*.log.*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
.coverage/
.nyc_output/

# Development scripts (keep local only)
start-emulator.bat
start-emulator.sh
main_1.py
*.log
npm-debug.log*
yarn-debug.log*

# Misc
.firebase/
*.local
